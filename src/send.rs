// send.rs: placeholder for raw-packet and UDP send implementations

pub async fn send_dns_packet(_domain: &str, _resolver: &str) -> Result<(), Box<dyn std::error::Error>> {
    // TODO: implement raw-packet (pcap) send & UDP fallback
    Ok(())
}
